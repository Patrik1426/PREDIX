# ============================================================
# PREDIX — Plantilla de Variables de Entorno
# Sistema Estatal de Inteligencia para Seguridad Publica
# ============================================================
# Copie este archivo como .env y configure los valores

# -- Base de Datos (OBLIGATORIO) --
DATABASE_URL=mysql://usuario:contrasena@localhost:3306/predix_db

# -- Autenticacion (OBLIGATORIO) --
JWT_SECRET=genere_una_clave_aleatoria_de_64_caracteres_aqui

# -- Boveda de Secretos (OBLIGATORIO) --
VAULT_MASTER_KEY=genere_una_clave_hex_de_64_caracteres_aqui

# -- Demo (presentaciones) --
# "true" habilita el switch de sesion simulada en los tabs Admin/Integracion.
# DEBE quedar "false" o ausente en produccion: nunca desactiva el gate real,
# solo permite alternar la vista con/sin sesion sin pasar por OAuth.
VITE_DEMO_MODE=false

# -- OAuth (Configurar con su proveedor de identidad) --
VITE_APP_ID=su_app_id
OAUTH_SERVER_URL=https://su-servidor-oauth.com
VITE_OAUTH_PORTAL_URL=https://su-portal-login.com

# -- Informacion del Propietario --
OWNER_OPEN_ID=id_del_propietario
OWNER_NAME=Nombre del Administrador

# -- APIs Externas (Opcional) --
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=

# -- Almacenamiento S3 (Opcional) --
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=predix-storage

# -- Puerto del Servidor (Opcional) --
PORT=3000
